<div id="btn-node-tree" class="top-offset" btn-jstree btn-jstree-change="reload">
    <ul>
        {% for node in topNodes %}
            {% include 'BtnNodeBundle:NodeControl:_tree.html.twig' %}
        {% endfor %}
    </ul>
</div>
<div class="top-offset">
    <a href="{{ url('btn_node_nodecontrol_new') }}" class="btn btn-primary btn-sm">{% trans %}btn_node.create_new_menu{% endtrans %}</a>
</div>
